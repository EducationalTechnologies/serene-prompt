import 'package:flutter/cupertino.dart';

class LexicalDecisionTaskExplanationScreen extends StatelessWidget {
  const LexicalDecisionTaskExplanationScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hier eine Erklärung wie das LDT funktioniert"),
    );
  }
}
